{
  "name": "sjioc-car-lookup",
  "version": "1.0.0",
  "description": "SJIOC Car Lookup Chatbot with Admin Panel",
  "main": "index.html",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp index.html styles.css script.js members_data.json members_data.csv public/ && ls -la public/",
    "start": "vercel dev"
  },
  "dependencies": {
    "multiparty": "^4.2.3"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "chatbot",
    "car-lookup",
    "church",
    "member-management"
  ],
  "author": "SJIOC",
  "license": "MIT"
}